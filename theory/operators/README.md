# Operators

This directory is reserved for operator-focused theory extracts and curated
operator documentation.

During the first public import, operator lineage remains preserved in
`archive/lineage/` and supporting formal materials remain under `theory/`.
