# Core

This layer contains imported executable code, simulation scripts, configuration,
and the minimal original data payload retained for reproducibility in the first
public import.

The initial public import preserves the original `Metatime-main` code and data
structure as faithfully as possible while removing technical build noise.
