# Validation

This layer preserves the auditability of the public repository.

- `manifests/` contains tree, merge, and checksum manifests.
- `reports/` contains human-readable validation and scan reports.
- `provenance/` contains machine-readable validation records and summaries.
- `debt_register/` contains the debt register and its deltas.
